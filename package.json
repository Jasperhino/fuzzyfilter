{
  "name": "fuzzyfilter-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --filter '*' dev",
    "test": "bun --filter fuzzyfilter test",
    "test:all": "bun test"
  },
  "workspaces": [
    "packages/*",
    "example/*"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  }
}